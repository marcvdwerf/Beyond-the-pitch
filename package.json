{
  "name": "beyond-the-pitch-booking",
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p public && cp booking.html public/index.html"
  },
  "dependencies": {
    "stripe": "^14.0.0"
  }
}
