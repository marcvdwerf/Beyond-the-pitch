{
  "name": "mijn-booking-site",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "mkdir -p public && cp index.html public/"
  },
  "dependencies": {
    "stripe": "^14.21.0"
  }
}
